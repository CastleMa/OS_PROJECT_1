#ifndef STRUCT_HPP
#define STRUCT_HPP

#include <string>

//structs
struct ChatArgs {
    std::string user;
    std::string dest;
    bool manual_mode;
    bool bot_mode;
};

#endif //structs are already defined